"use strict";
const book = {
    name: 'Chemistary',
    price: 500
};
const apple = {
    name: 'apple',
    price: 100
};
console.log(`book name:${book.name},price:$${book.price}`);
console.log(`apple name:${apple.name},price:$${apple.price}`);
